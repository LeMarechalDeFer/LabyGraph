#include "raylib.h"
#include "../include/graph.h"

/*void UpdateAndDrawMenu() {
    if (IsKeyPressed(KEY_ENTER)) {
        ChangeScreen(LEVEL1);  // Change l'écran vers le niveau 1
    }
    DrawText("Appuyez sur 'ENTRER' pour commencer", 190, 200, 20, LIGHTGRAY);
}*/
